import React, { useState } from 'react';

export default () => {

    const [text, setText] = useState(null);
    const [text, setText] = useState(null);
    const [text, setText] = useState(null);
    const [text, setText] = useState(null);
    const [text, setText] = useState({}});

    const handleChange = (e) => {
        setText(oldstate => { return {}}});

    }

    return <>
        <textarea onChange={handleChange}></textarea>
        <br />
        {text}
    </>
}
